# Projects

Per-project memory lives here. Each subdirectory is one project.

```
projects/
└── <project-name>/
    └── notes.md
```

Create a new folder when starting a project:
```bash
mkdir memory/projects/my-project
```
